DATABASE_URL="postgres://secureshare:secureshare@localhost:5432/secureshare"
REDIS_URL=redis://localhost:6379

# Optional. Base URL for note links in API responses. If unset: production -> https://1note.xyz, local -> http://localhost:5173 (secret-share frontend).
# PUBLIC_APP_URL=https://your-frontend.com
# PUBLIC_APP_URL=http://localhost:5173

# Required. 32-byte key: hex (64 chars) or base64 (44 chars). Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
